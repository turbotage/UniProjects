

#define SIZE_N 2

int main() {

	double* series = calloc(8 * SIZE_N, sizeof(double));
	int M = 8192;
	int bit_size = 64;

	char* message = calloc(SIZE_N / 8, sizeof(char));

	for (int i = 0; i < SIZE_N / 8; ++i) {
		for (int j = 0; j < SIZE_N; ++j) {
			
		}
	}

}


